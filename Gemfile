# frozen_string_literal: true

ASpaceGems.setup if defined? ASpaceGems

source 'https://rubygems.org'

# Including "jruby-openssl" gem, as otherwise attempting to connect to
# an HTTPS server may result in an SSL handshare error, due to lack
# of appropriate SSL/TLS ciphers
gem 'jruby-openssl'